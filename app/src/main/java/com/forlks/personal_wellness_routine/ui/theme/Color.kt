package com.forlks.personal_wellness_routine.ui.theme

import androidx.compose.ui.graphics.Color

// ── WellFlow Brand Colors — Sage & Mint (v0.0.3) ───────────────────────────
// 기존 Material Green 500 계열에서 더 편안한 세이지 그린 계열로 전환
val WellGreen       = Color(0xFF5E8B63)   // 세이지 그린 (차분하고 자연스러운 느낌)
val WellGreenLight  = Color(0xFF8FB48F)   // 소프트 세이지 (배경·컨테이너용)
val WellGreenDark   = Color(0xFF3A6640)   // 딥 포레스트 (강조·다크모드)
val WellTeal        = Color(0xFF4D9489)   // 소프트 티얼
val WellGold        = Color(0xFFD4A030)   // 따뜻한 골드
val WellOrange      = Color(0xFFE06B47)   // 어스 오렌지

// ── Surface / Background ─────────────────────────────────────────────────────
val WellBackground      = Color(0xFFF3F7F3)   // 아주 연한 세이지 흰색
val WellSurface         = Color(0xFFFFFFFF)   // 순백
val WellSurfaceVariant  = Color(0xFFE9F0EA)   // 연한 세이지 서피스

// ── Text ─────────────────────────────────────────────────────────────────────
val WellOnSurface        = Color(0xFF253525)   // 소프트 다크 (순검정보다 부드러움)
val WellOnSurfaceVariant = Color(0xFF5E7862)   // 중간 세이지 텍스트

// ── Emotion colors ────────────────────────────────────────────────────────────
val EmotionPositive = Color(0xFF5E8B63)   // 세이지 그린
val EmotionNeutral  = Color(0xFFD4A030)   // 따뜻한 황색
val EmotionNegative = Color(0xFFD45C5C)   // 소프트 레드

// ── Temperature colors ────────────────────────────────────────────────────────
val TempWarm   = Color(0xFFE07B5A)   // 따뜻한 살구
val TempNormal = Color(0xFF5C9FC0)   // 부드러운 블루
val TempCool   = Color(0xFF7A9FA8)   // 쿨 그레이블루
val TempCold   = Color(0xFF8A74C2)   // 소프트 퍼플

// ── Character level colors ────────────────────────────────────────────────────
val LevelGold   = Color(0xFFD4A030)   // 골드
val LevelPurple = Color(0xFF9C7AC0)   // 소프트 퍼플
val LevelGray   = Color(0xFFC0BDBD)   // 중립 그레이
